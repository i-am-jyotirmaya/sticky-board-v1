import Lane from "./Lane/Lane";

const Play = () => {
  return (
    <div
      style={{
        height: "100%",
        padding: "1rem",
      }}
    >
      <Lane />
    </div>
  );
};

export default Play;
